{
    "name": "cm2040-event-manager-coursework",
    "version": "1.0.0",
    "description": "A template for the mid-term assignment",
    "main": "index.js",
    "scripts": {
        "test": "node --check index.js && node --check routes/events.js && node --check routes/auth.js && node --check routes/users.js && node --check models/eventsModel.js && node --check seed.js",
        "build-db": "node scripts/build-db.js",
        "clean-db": "rm database.db #remove the old database",
        "build-db-win": "sqlite3 database.db < db_schema.sql",
        "clean-db-win": "del database.db",
        "start": "node index.js"
    },
    "author": "Kwok Whai Hoe",
    "license": "ISC",
    "dependencies": {
        "bcrypt": "^6.0.0",
        "ejs": "^3.1.8",
        "express": "^4.18.2",
        "express-session": "^1.19.0",
        "sqlite3": "^5.1.2"
    },
    "engines": {
        "npm": ">=8.0.0",
        "node": ">=16.0.0"
    }
}
